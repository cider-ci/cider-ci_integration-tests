scripts:

  test:

    body: |
      #!/usr/bin/env bash
      set -eux
      export PATH=~/.rubies/$RUBY/bin:$PATH
      cd integration-tests
      xvfb-run -a -e /dev/stdout bundle exec rspec $CIDER_CI_TASK_FILE
